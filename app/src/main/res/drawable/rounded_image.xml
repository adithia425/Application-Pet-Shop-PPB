<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="16dp" /> <!-- Menetapkan radius sudut untuk membuat tepi melingkar -->
    <solid android:color="@android:color/white"/> <!-- Warna latar belakang -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white" /> <!-- Stroke untuk tepi -->
</shape>